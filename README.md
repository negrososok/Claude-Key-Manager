# Claude Key Manager

Public repository shell for Claude Manager / Claude Key Manager.

Source publication is intentionally staged. Local runtime files, release builds,
logs, databases, secrets, prompt drafts and AI workspace artifacts are ignored by
`.gitignore` before any full source release is prepared.
